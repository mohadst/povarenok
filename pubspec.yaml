name: cooking_assistant
description: "Приложение-помощник для готовки"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  speech_to_text: ^7.0.0
  flutter_tts: ^4.2.0  # Обновленная версия
  http: ^0.13.6
  flutter_dotenv: ^5.0.2
  flutter_svg: ^2.0.9
   

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

assets:
    - assets/cherry.png
